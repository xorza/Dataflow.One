<Window x:Class="csso.NodeRunner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfApp1"
        xmlns:nodeRunner="clr-namespace:csso.NodeRunner"
        Padding="0"
        Margin="0"
        Background="{x:Null}"
        VerticalContentAlignment="Stretch"
        HorizontalContentAlignment="Stretch">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Menu>
            <MenuItem Header="_File">
                <MenuItem Header="_New"  Click="New_MenuItem_OnClick"  />
                <MenuItem Header="_Open" Click="Open_MenuItem_OnClick"  />
                <MenuItem Header="_Save" Click="Save_MenuItem_OnClick"  />
                <Separator />
                <MenuItem Header="_Exit" Click="Exit_MenuItem_OnClick" />
            </MenuItem>
        </Menu>

        <nodeRunner:Overview
            x:Name="GraphOverview"
            Grid.Row="1"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch" />
    </Grid>
</Window>